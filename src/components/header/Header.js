import React from'react';
import Navigation from './Navigation.js';

const Header = (props)=>{
    
  

    return (
        <div>
            <Navigation/>
        </div>
    );
}

export default Header;